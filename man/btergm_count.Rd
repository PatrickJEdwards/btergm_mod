% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/btergm_count.R
\name{btergm_count}
\alias{btergm_count}
\title{Estimate a temporal ERGM for count-valued networks by bootstrapped count MPLE}
\usage{
btergm_count(
  formula,
  R = 500,
  response = "tie_count",
  offset = FALSE,
  returndata = FALSE,
  parallel = c("no", "multicore", "snow"),
  ncpus = 1,
  cl = NULL,
  control.ergm = NULL,
  usefastglm = FALSE,
  verbose = TRUE,
  reference = c("poisson", "geometric", "uniform", "binomial"),
  max.count = Inf,
  max.count.safety = 4,
  max.count.edgewise = TRUE,
  count.samples = Inf,
  must.count = 5,
  sample.size = Inf,
  sample.method = c("weighted", "random"),
  weight.type = c("TNT", "flatval"),
  regularization = c("none", "L2", "L1", "pseudoHuber", "L1pH"),
  regularization.param = c(1, 1e-04),
  coef.init = NULL,
  optim.method = "trust",
  WtSumAsSampSiz = TRUE,
  seed = NULL,
  prep.cores = 1,
  prep.parallel = c("auto", "fork", "psock", "serial"),
  prep.cl = NULL,
  mple.cores = 1,
  ...
)
}
\arguments{
\item{formula}{Formula for the temporal ERGM. The left-hand side should be
a list of count-valued network objects or matrices in chronological order.}

\item{R}{Number of temporal bootstrap replications.}

\item{response}{Character string giving the name of the edge attribute that
stores the count outcome. For example, \code{"count"}.}

\item{offset}{Logical. If \code{TRUE}, use structural-zero offset handling
from \code{tergmprepare()}. For the first implementation, this should
usually remain \code{FALSE}; user-supplied formula offsets will be handled
separately.}

\item{returndata}{Logical. Return prepared data rather than estimating the
model?}

\item{parallel}{Character. One of \code{"no"}, \code{"multicore"}, or
\code{"snow"}.}

\item{ncpus}{Number of CPU cores for temporal bootstrap parallelization.}

\item{cl}{Optional cluster object for \code{parallel = "snow"}.}

\item{control.ergm}{Optional ergm control object. Reserved for compatibility
with \code{btergm()}; not used directly by the count MPLE optimizer yet.}

\item{usefastglm}{Logical. Included for API compatibility with
\code{btergm()}. Count MPLE does not use a binomial GLM, so this is ignored
for now. The binary btergm() function eventually estimates a binomial 
GLM-style pseudolikelihood, where fastglm makes sense. The Huang-Butts 
count MPLE code instead builds a count pseudolikelihood and optimizes it 
using trust() or optim(), so there is no immediate GLM step to replace with 
fastglm.}

\item{verbose}{Logical. Print preprocessing and estimation messages?}

\item{reference}{Reference measure for the count ERGM. One of
\code{"poisson"}, \code{"geometric"}, \code{"uniform"}, or
\code{"binomial"}.}

\item{max.count}{Maximum possible count value. Use \code{Inf} for unbounded
count support.}

\item{max.count.safety}{Safety multiplier used when approximating the count
support.}

\item{max.count.edgewise}{Logical. Use edgewise count support truncation?}

\item{count.samples}{Maximum number of support values to use per edge
variable.}

\item{must.count}{Integer. Always include support values from 0 through this
value.}

\item{sample.size}{Number of edge variables to sample for count MPLE. Use
\code{Inf} to use all dyads.}

\item{sample.method}{Edge-variable sampling method. One of
\code{"weighted"} or \code{"random"}.}

\item{weight.type}{Weighting scheme when \code{sample.method = "weighted"}.
One of \code{"TNT"} or \code{"flatval"}.}

\item{regularization}{Regularization method. One of \code{"none"},
\code{"L2"}, \code{"L1"}, \code{"pseudoHuber"}, or \code{"L1pH"}.}

\item{regularization.param}{Numeric vector of regularization parameters.}

\item{coef.init}{Optional starting values for the optimizer.}

\item{optim.method}{Optimizer. Use \code{"trust"} for trust-region
optimization, or another method accepted by \code{optim()}.}

\item{WtSumAsSampSiz}{Logical. Whether inverse weights should sum to the
sampled edge-variable count.}

\item{seed}{Optional random seed.}

\item{prep.cores}{Number of cores used during count-MPLE preparation,
including count support construction, reference-measure ratios, and
change-score computation. This happens before the temporal bootstrap.}

\item{prep.parallel}{Optional preparation-stage parallelization mode.
Reserved for preparation workflows that parallelize across time periods.}

\item{prep.cl}{Optional cluster object for preparation-stage parallelization.}

\item{mple.cores}{Number of cores used inside each count-MPLE fit for
edge-variable change-score calculations. Keep this at 1 when using outer
bootstrap parallelization to avoid nested parallelism.}

\item{...}{Additional arguments passed later to the count-MPLE optimizer.}
}
\value{
A fitted \code{btergm_count} object. In the current scaffold,
  estimation is not yet implemented.
}
\description{
Estimate a temporal exponential-family random graph model for count-valued
networks using temporal bootstrapping and count-valued maximum
pseudolikelihood estimation.
}
